# Pipeline / scripts / asset-bootstrap dependencies.
# (The API runtime has its own pinned set — see requirements-api.txt.)
#
# Target runtime: Python 3.12 — the EDA/FE pipeline was validated on Kaggle's 3.12.
# These were NOT installed in the local 3.14 venv; create a dedicated 3.12 venv to
# run/reproduce the pipeline. Versions below target 3.12 and are not yet CI-lock-verified.

# --- data + modeling (EDA + feature engineering) ---
pandas==2.2.3
numpy==1.26.4
scipy==1.13.1
scikit-learn==1.5.2
lightgbm==4.5.0
pyarrow==17.0.0          # parquet engine used by pandas read_parquet / to_parquet
pygeohash==1.2.0         # geohash-7 cell encoding

# --- spatial statistics (Getis-Ord Gi* hotspot significance) ---
libpysal==4.12.1
esda==2.6.0

# --- visualisation (EDA notebooks) ---
matplotlib==3.9.2
folium==0.17.0

# --- remote Kaggle kernel driver + notebook assembly ---
requests==2.32.3
websocket-client==1.8.0
nbformat==5.10.4

# --- asset bootstrap: download model + scored artifacts from Google Drive ---
gdown==5.2.0
